import * as React from "react";
import { Grid } from "@mui/material";
export const PlacesList = () => {
  return (
    <>
      <Grid xs={12} style={{ minHeight: "90vh", backgroundColor: "gray" }}>
        MAHOE
      </Grid>
    </>
  );
};
